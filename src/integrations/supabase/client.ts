// Supabase client is no longer used. The project has migrated to MongoDB + JWT admin auth.
// If you need the original client, see `archive/supabase/client.ts` for a copy.

// NOTE: Don't delete this file until you've verified there are no remaining imports.
export const supabase = null as any;
